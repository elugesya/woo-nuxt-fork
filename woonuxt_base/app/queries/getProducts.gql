query getProducts($after: String, $first: Int = 100, $orderby: ProductsOrderByEnum = DATE) {
  products(
    first: $first
    after: $after
    where: { visibility: VISIBLE, minPrice: 0, orderby: { field: $orderby, order: DESC }, status: "publish" }
  ) {
    pageInfo {
      hasNextPage
      endCursor
    }
    nodes {
      name
      slug
      type
      databaseId
      id
      averageRating
      reviewCount
      ...Terms
      ...ProductCategories
      ...ProductBrands
      ...SimpleProduct
      ...VariableProduct
      ...ExternalProduct
    }
  }
}
